defaults:
  - /my_project/schemas/ExampleEntrypointConf@_here_  # If you do not add any custom arguments, using EntrypointConf also works
  - contexts: my_contexts

greeting: Hey from yaml and with specified context
